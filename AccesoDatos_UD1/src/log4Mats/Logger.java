package log4Mats;

/**
 * Clase que define un logger por nivel de prioridad de errores:
 * Trace, Debug, Info, Warn, Error. (de menor a mayor prioridad)
 *
 * Los logs se guardan en .../logs/nombreTienda.log y cuando se llenan se rota fichero.
 */


public class Logger {

    // Crear desde una clase LogManager (un builder) que nos de
    // private static final Logger logger = LogManager.getLogger(Log4Mats.class);
    // El Log4Mats.class es el xml

    // error(String error)

    // warn(String warn)

    // info(String info)

    // debug(String debug)

    // trace(String trace)

}
